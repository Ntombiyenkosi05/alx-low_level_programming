#include "main.h"
/**
 * mul - Multiplies two integers
 * @x: The first number to be multiplied
 * @y: The second number to be multiplied
 * Return: The product of mul
 */
int mul(int x, int y)
{
	return (x * y);
}
